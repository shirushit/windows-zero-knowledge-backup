# מדריך למשתמש: תוכנת גיבוי מאובטח ללא ידיעה (Zero-Knowledge)

ברוכים הבאים לתוכנת הגיבוי המאובטח עבור Windows. תוכנה זו נבנתה במטרה להעניק לכם פרטיות מלאה, אבטחה בלתי מתפשרת וגיבוי אמין של הקבצים החשובים שלכם לענן (באמצעות אחסון טלגרם פרטי), ללא תלות בשרתים מרכזיים או חברות צד שלישי.

---

## 1. מה זה אומר "אפס ידיעה" (Zero-Knowledge)?

* **ההצפנה מתבצעת כולה במחשב שלכם בלבד:** לפני שקובץ כלשהו נשלח לרשת, הוא נחתך לחלקים קטנים (Chunks) ומוצפן באמצעות אלגוריתם ההצפנה המוביל בעולם (`XChaCha20-Poly1305`).
* **אף אחד לא יכול לראות את המידע שלכם:** לא טלגרם, לא ספק האינטרנט שלכם, ואפילו לא מפתחי התוכנה. שמות הקבצים, מבנה התיקיות ותוכן הקבצים חסויים ומוצפנים לחלוטין.
* **רק לכם יש את המפתח:** הגישה לקבצים אפשרית אך ורק באמצעות סיסמת המאסטר שלכם או ביטוי השחזור הסודי שלכם.

> [!WARNING]
> **אזהרת אבטחה קריטית:**
> מכיוון שהתוכנה פועלת בארכיטקטורת "אפס ידיעה", אין בתוכנה שום "איפוס סיסמה באימייל" או דלת אחורית. אם תשכחו את סיסמת המאסטר **וגם** תאבדו את ביטוי השחזור, **לא תהיה שום דרך בעולם לשחזר את הקבצים שלכם**. שמרו עליהם במקום מוגן!

---

## 2. התקנה והפעלה ראשונית

התוכנה מופצת כקובץ הפעלה עצמאי ובודד עבור Windows (`BackupApp.UI.exe`).

### דרכי הפעלה:
1. **הפעלה ישירה (ללא התקנה):**
   * הורידו את `BackupApp.UI.exe`.
   * הניחו אותו בכל תיקייה שתרצו (למשל `C:\Programs\BackupApp`) והפעילו אותו בלחיצה כפולה.
2. **התקנה מסודרת במחשב (מומלץ):**
   * הריצו את סקריפט ההתקנה `scripts/install.ps1`.
   * הסקריפט יתקין את התוכנה תחת תיקיית המשתמש המאובטחת (`%LOCALAPPDATA%\Programs\BackupApp`) וייצור קיצור דרך בתפריט 'התחל'.

---

## 3. הגדרת אבטחה ראשונית: סיסמה וביטוי שחזור

בהפעלה הראשונה התוכנה תבקש מכם ליצור את תשתית ההצפנה שלכם:

1. **בחירת סיסמת מאסטר (Master Password):**
   * בחרו סיסמה חזקה ומורכבת (מומלץ מעל 12 תווים, המשלבים אותיות, מספרים וסימנים).
   * הסיסמה משמשת לפתיחה השוטפת של התוכנה ולנעילת מפתחות ההצפנה.
2. **קבלת ביטוי השחזור (Recovery Phrase):**
   * התוכנה תציג בפניכם ביטוי שחזור המורכב ממילים באנגלית (תקן בינלאומי BIP-39).
   * **רשמו את המילים לפי הסדר על דף נייר** ושמרו אותו בכספת או במקום מסתור בטוח.
   * ביטוי זה הוא "גלגל ההצלה" שלכם: אם המחשב יישרף, ייגנב או שתשכחו את הסיסמה, ביטוי זה יאפשר לכם לשחזר הכל מכל מחשב חדש בעולם.

---

## 4. הגדרת יעד האחסון (ענן טלגרם פרטי)

התוכנה משתמשת בתשתית הענן החינמית והבלתי מוגבלת של טלגרם כאחסון אובייקטים מוצפנים. הנה איך להגדיר זאת תוך 2 דקות:

### שלב א': יצירת בוט בטלגרם
1. פתחו את אפליקציית טלגרם וחפשו את המשתמש הרשמי `@BotFather`.
2. שלחו לו את הפקודה `/newbot`.
3. בחרו שם לבוט (למשל: `My Backup Bot`) ושם משתמש שמסתיים ב-`bot` (למשל: `my_safe_backup_2026_bot`).
4. `BotFather` יחזיר לכם מחרוזת ארוכה הנקראת **Bot Token** (למשל: `123456789:ABCdefGhIJKlmNoPQRsTUVwxyZ`).

### שלב ב': יצירת ערוץ פרטי לקבלת הגיבויים
1. בטלגרם, צרו **ערוץ חדש (Channel)** או **קבוצה חדשה (Group)** והגדירו אותה כ**פרטית (Private)**.
2. הוסיפו את הבוט שיצרתם כמנהל (Administrator) בערוץ.
3. כדי למצוא את מזהה הערוץ (**Chat ID**):
   * שלחו הודעת בדיקה כלשהי בערוץ.
   * היכנסו בדפדפן לכתובת: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
   * חפשו בתוצאה את השדה `"id"` תחת `"chat"`. זהו מספר שמתחיל בדרך כלל במינוס (למשל: `-1001987654321`).

### שלב ג': הזנה בתוכנה
* בלשונית **הגדרות (Settings)** בתוכנה, הזינו את ה-**Bot Token** ואת ה-**Chat ID**, ולחצו על **בדוק חיבור ושמור**.

---

## 5. ביצוע גיבוי ראשון ושוטף

1. **בחירת תיקיות לגיבוי:**
   * בלשונית הראשית, לחצו על **הוסף תיקייה לגיבוי** ובחרו את התיקיות שברצונכם לגבות (למשל `מסמכים`, `תמונות`, `שולחן העבודה`).
2. **הפעלת הגיבוי:**
   * לחצו על כפתור **התחל גיבוי כעת**.
   * התוכנה תסרוק את הקבצים, תחשב חתימות דיגיטליות, תבצע מניעת כפילויות (Deduplication), תצפין כל מקטע ותעלה אותם לענן הטלגרם שלכם.
3. **גיבויים עתידיים (אינקרמנטליים ומהירים):**
   * בכל גיבוי נוסף שתפעילו, התוכנה תזהה אך ורק קבצים חדשים או מקטעים שהשתנו, ותעלה רק אותם. גיבוי שוטף לוקח בדרך כלל שניות בודדות בלבד!
4. **עמידות להפרעות וניתוקי רשת:**
   * אם המחשב כבה, נותק מהאינטרנט או שהתוכנה נסגרה באמצע הגיבוי — אין מה לחשוש! בהפעלה הבאה התוכנה תמשיך בדיוק מהנקודה שבה הפסיקה מבלי להעלות שוב מידע שכבר הועלה.

---

## 6. שחזור נתונים מלא במקרה של אסון (Disaster Recovery)

אם המחשב המקורי הלך לאיבוד, נגנב או נפגע לחלוטין, ואתם יושבים מול מחשב חדש ונקי:

1. הורידו את `BackupApp.UI.exe` למחשב החדש.
2. במסך הפתיחה, בחרו באפשרות **שחזור מאסון (Disaster Recovery)**.
3. הזינו את ה-**Bot Token** ואת ה-**Chat ID** של ערוץ הטלגרם שבו שמורים הגיבויים.
4. הזינו את **ביטוי השחזור (Recovery Phrase)** ששמרתם (או את סיסמת המאסטר המקורית).
5. התוכנה תתחבר ישירות לענן הטלגרם, תאתר את חבילות המניפסט המוצפנות, ותציג לכם את עץ הקבצים המקורי שלכם.
6. בחרו תיקיית יעד לשחזור (למשל `D:\RestoredFiles`) ולחצו על **בצע שחזור מלא**.
7. התוכנה תוריד את כל המקטעים, תוודא את תקינותם ומקוריותם מול תגי האימות המוצפנים, ותשחזר את כל הקבצים שלכם במלואם — כולל שמות הקבצים, המבנה ההיררכי ותאריכי היצירה/עדכון המקוריים!

---

## 7. פתרון תקלות ושאלות נפוצות

* **ש: קיבלתי הודעת שגיאה על "סיסמה שגויה או נתונים פגומים":**
  * ת: ודאו שהקלדתם את הסיסמה במדויק (שימו לב ל-Caps Lock ולשפת המקלדת). בהצפנת אפס-ידיעה, סיסמה לא נכונה אפילו באות אחת מונעת פתיחה של מפתחות ההצפנה.
* **ש: חלק מהקבצים שלי פתוחים בתוכנות אחרות (כמו Word או Excel). האם הם יגובו?**
  * ת: התוכנה מטפלת בנעילת קבצים בטוחה. אם קובץ נעול לחלוטין לקריאה בלעדית על ידי תוכנה אחרת, התוכנה תרשום אזהרה מסודרת ביומן, תמשיך לגבות את שאר הקבצים ללא עצירה, ותגבה את הקובץ הנעול בסבב הגיבוי הבא.
* **ש: מה קורה אם מישהו פורץ לערוץ הטלגרם שלי?**
  * ת: התוקף יראה אך ורק קבצים בינאריים סתמיים עם שמות אקראיים ומוצפנים. ללא סיסמת המאסטר או ביטוי השחזור שלכם, הקבצים הם חסרי ערך לחלוטין ובלתי ניתנים לפענוח.

---

**תמיד כאן לשמור על הנתונים שלכם מוגנים, פרטיים ובשליטתכם המלאה!**
